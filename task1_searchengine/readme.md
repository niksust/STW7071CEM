# Task 1
## Setup

```bash
1. 
python -m venv .venv
#macos: python3 -m venv .venv

2.
# Windows: .venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

3.
pip install -r requirements.txt

4. 
#run app
streamlit run app.py

5. Optional crawler run
#If res.json is not available
python -m crawler